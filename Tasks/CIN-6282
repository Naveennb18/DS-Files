# Steps to set up

  - Install Docker


  - Run the following commands

    docker pull registry.prod.infra.deposit/infrastructure/docker-dind:latest

    docker images

    docker run -it --privileged -v $(pwd):/test registry.prod.infra.deposit/infrastructure/docker-dind:latest

    docker ps

    docker exec -it <container-id> /bin/ash

    /usr/local/share/ca-certificates # cd /

      docker run -it --privileged -v $(pwd):/test registry.prod.infra.deposit/infrastructure/docker-kitchen/docker-kitchen:latest

      docker ps

      docker exec -it <container-id> /bin/ash

      /tmp # cd /test/

      /test/ # kitchen test

-------------------------------------------------------------------------------------------------------------------------------------------------------

https://archive.repo.saltstack.com/py3/debian/10/amd64/archive/3000/  --> New URL

http://repo.saltstack.com/py3/debian/10/amd64/3000/ --> Old URL
